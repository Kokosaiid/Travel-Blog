<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>✈️ Travel Blog</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <header>
      <h1>🌍 Travel Blog</h1>
      <a href="/new" class="btn">+ Share Your Story</a>
    </header>

    <main>
      <% if (posts.length === 0) { %>
      <p class="empty">
        No adventures yet. Start your journey and write the first story!
      </p>
      <% } else { %>
      <div class="post-list">
        <% posts.forEach(post => { %>
        <article class="post">
          <h2><%= post.title %></h2>
          <p><%= post.content %></p>
          <div class="actions">
            <a href="/posts/<%= post.id %>/edit" class="btn-edit">✏️ Edit</a>
            <form
              action="/posts/<%= post.id %>?_method=DELETE"
              method="POST"
              style="display: inline"
            >
              <button class="btn-delete">🗑 Delete</button>
            </form>
          </div>
        </article>
        <% }) %>
      </div>
      <% } %>
    </main>
  </body>
</html>
